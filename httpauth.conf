auth.require = (
    "/" => (
        "method" => "basic",
        "realm" => "IkiWiki Access",
        "require" => "valid-user"
    )
)
auth.backend = "htpasswd"
auth.backend.htpasswd.userfile = "/home/ikiwiki/conf/htpasswd"
